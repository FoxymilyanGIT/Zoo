# 🚀 Запуск с ngrok (для учебных целей)

## 📋 Предварительные требования

1. **PostgreSQL** должен быть запущен локально
2. **ngrok** должен быть установлен
3. **Java 17+** должен быть установлен

## ⚡ Быстрый запуск

### Вариант 1: Простой скрипт (Windows)
```bash
# Дважды кликните на run-with-ngrok.bat
# Или запустите в командной строке:
run-with-ngrok.bat
```

### Вариант 2: Ручной запуск

#### Шаг 1: Запустите backend
```bash
cd backend
java -jar target/zoopark-backend-0.0.1-SNAPSHOT.jar
```

#### Шаг 2: Запустите ngrok (в новом терминале)
```bash
ngrok http 8080
```

#### Шаг 3: Скопируйте публичный URL
```
Forwarding    https://abc123.ngrok.io -> http://localhost:8080
```

## 🌐 Доступ к приложению

### После запуска скрипта:

1. **Скопируйте ngrok URL** из терминала ngrok:
   ```
   Forwarding    https://abc123.ngrok.io -> http://localhost:8080
   ```

2. **Настройте frontend для работы с backend:**
   ```bash
   # Откройте новый терминал и выполните:
   cd frontend
   echo "VITE_API_BASE_URL=https://abc123.ngrok.io" > .env.local
   ```

3. **ИЛИ настройте переменную окружения:**
   ```bash
   # В терминале frontend выполните:
   set VITE_API_BASE_URL=https://abc123.ngrok.io
   # И перезапустите npm run dev
   ```

### Доступ:
- **Frontend (сайт):** `http://localhost:5173`
- **Backend API:** `https://abc123.ngrok.io/api/health`
- **ngrok Dashboard:** `http://localhost:4040`

## 🛠️ Установка ngrok

### Windows (через Chocolatey):
```bash
choco install ngrok
```

### Ручная установка:
1. Скачайте с [ngrok.com/download](https://ngrok.com/download)
2. Распакуйте в папку из PATH
3. Авторизуйтесь: `ngrok config add-authtoken YOUR_TOKEN`

## 📝 Локальная разработка

Для обычной разработки без ngrok:
```bash
# Backend
cd backend
mvn spring-boot:run

# Frontend (в новом терминале)
cd frontend
npm run dev
```

## ⚠️ Важно

- **ngrok URL меняется** при каждом перезапуске
- **Бесплатный план** имеет ограничения по трафику
- **Для production** используйте Railway или Heroku

## 🔧 Troubleshooting

### Backend не запускается:
```bash
# Проверьте PostgreSQL
Get-Service postgresql*

# Проверьте порт 8080
netstat -ano | findstr :8080
```

### ngrok не работает:
```bash
# Проверьте установку
ngrok version

# Авторизуйтесь
ngrok config add-authtoken YOUR_TOKEN
```
